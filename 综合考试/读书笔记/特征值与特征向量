很多应用问题都涉及到将一个线性变换重复做用到一个向量上，求解这类问题的关键是针对算子选择一个在某种意义下很自然的坐标系，并使得包含该算子的计算得以简化。对应于这组新的基向量（特征向量），关联一个缩放因子（特征值）来代表自然频率。

某个向量作为初始向量的话，他可以分解成特征向量的线性组合。在多次操作之后（$A^n$），该向量会被分成两个部分的和，也就是$\lambda_i^n$倍的特征向量。

特征值与1的关系是很重要的。关系到特征向量最后的收敛性。

## 对角化

- 一个$n\times n$的矩阵$A$是可对角化的，当且仅当它有$n$个线性无关的特征向量。